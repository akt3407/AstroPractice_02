---
interface Props {
  title: string;
  description: string;
  class?: string;
}
const { title, description, class: className } = Astro.props;
---

<div class={className}>
  <h3 class="text-16b/normal">{title}</h3>
  <p class="text-12/[1.2] mt-12">{description}</p>
</div>
